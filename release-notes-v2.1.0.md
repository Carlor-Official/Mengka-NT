# 萌卡 NT v2.1.0

## 插件市场与一体化部署

- 插件市场重新接入官网审核发布。开发者通过表单填写平台下载地址、启动文件和接入信息，审核通过后进入框架市场，无需编写 JSON 或手填安装包指纹。
- 下载时自动匹配 Windows AMD64、Linux AMD64 或 Linux ARM64，部署并启动插件，同时生成对应的插件对接服务、端口和令牌。
- 安装进度和管理操作集中在插件市场“已安装”卡片，支持管理后台、启停、日志、取消、重试与保留业务数据卸载。
- 数据配置支持自定义存储目录、迁移并保留原目录，以及声明允许插件使用的其他目录。
- 优化插件卡片、分组切换和按钮布局，恢复作者外显名称与 Logo 展示。

## 账号登录与等级加速

- Linux 账号登录新增方式选择。同 QQ 安卓账号已登录时可免扫快捷登录，否则保留人工扫码。
- “电脑QQ在线”支持通过安卓授权登录 Linux，可手动补挂或加入框架自动计划。已有 Linux 在线会话直接复用，首次需要时自动建立独立 Linux 账号和设备配置。
- 登录成功后继续累计在线时长，加速进度仍以 QQ 返回结果为准。

- 等级任务新增“会员签到”分组，独立开关与框架自动计划共用。显示实际业务结果并保存执行状态，避免重复领取；上游已结束的活动明确标为不可用。
- 修复 Android 设置好友备注返回成功却未生效的问题。Android、Linux 均在服务器回读确认后返回成功；缺失或非字符串备注不再误清空。

## 文档与 SDK

- 同步官网 API 文档和独立 Node.js SDK，补齐共享等级任务管理及一体化插件接入说明。
- 部署教程按 2.1.0 重写，使用测试服务器临时实例的真实截图，命令代码块支持复制。

## 升级说明

1. 停止框架，备份完整程序目录、数据目录及自定义插件存储目录，再更新程序、网页资源与随包协议文件。保留实例配置、数据库、凭据和插件业务数据。
2. 旧插件市场发布格式停用，旧条目下架保留资料。开发者补齐新版表单后重新提交审核，原审批不能直接恢复上架。安装与配置说明、数据与目录说明均为选填。
3. 已有外部 WS 插件继续通过“插件对接”管理，不自动迁移到托管实例；避免外部和托管副本同时使用同一份业务数据。
4. 从 HTTPS 框架打开插件后台前，请按[一体化插件部署说明](docs/managed-plugins.md)配置独立 HTTPS 插件网关；同机进程由框架管理时，systemd 使用 `KillMode=mixed`。
5. 声明额外目录需要插件适配读取，不构成操作系统沙箱。当前版本不提供已安装插件的在线升级和自动版本回滚；重新安装前保留业务备份。

[部署教程](https://mknt.net/guide/) · [API 文档](https://mknt.net/api/) · [Node.js SDK](https://github.com/Carlor-Official/Mengka-NT-SDK-Nodejs)
