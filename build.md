# 发行版更新日志编写规范

萌卡 NT 不开源，仓库只发布发行版。用户看到的关于本次更新的全部信息，就是 Release 里的这份更新日志，所以它面向**使用者**，不是面向开发者的提交记录。

## 核心原则

**只写用户能感知的变化。** 判断标准很简单：用户不看代码，他能不能察觉到这条？察觉不到就不要写。

**不暴露内部实现。** 不出现文件名、函数名、内部模块名、数据表和字段名、第三方接口的内部字段名，也不描述代码是怎么改的。

**公开接口的变化要写。** 插件 API、配置项、命令行参数这些是对外契约，插件作者依赖它们，属于用户必须知道的内容。写"这个 API 现在怎么用"，不写"它内部怎么实现的"。

## 该写什么

| 类型 | 说明 |
| --- | --- |
| 新增功能 | 用户能用到的新能力，说清楚它能做什么 |
| 功能优化 | 体验、性能、稳定性上的可感知改善 |
| 问题修复 | 描述**用户遇到的现象**，不是代码里的成因 |
| 接口变更 | 插件 API 的新增、参数变化、返回结构变化 |
| 升级提示 | 需要用户动手操作的事项，必须写 |

## 不该写什么

- 内部重构、代码整理、命名调整、注释和文档改动
- 依赖升级、构建脚本、打包流程、目录结构调整
- 用户无法触发或察觉的边界情况修复
- 内部实现细节，哪怕它是这次改动的主要工作量

## 正反对照

写现象和用法，不写成因和实现：

| ❌ 不要这样写 | ✅ 应该这样写 |
| --- | --- |
| 修复协议序号从 1 开始导致索引偏移的问题 | 修复部分账号协议识别错误导致无法登录的问题 |
| `get_sms` 去掉外层包装，改为透传上游响应 | 插件 API `get_sms` 现在会完整返回验证信息，包括需要发送的短信内容和接收号码 |
| 重构安全验证响应的状态解析逻辑 | 提升安全验证响应的兼容性，减少验证状态识别错误 |
| 更新检查地址迁移至新的托管服务 | 优化更新检查的连接稳定性 |
| 修复 `verify_type` 硬编码导致无法选择验证方式的问题 | 新增「发送短信」验证方式，可用密保手机发送指定短信完成验证 |

注意最后一条：同一个改动，从用户视角看往往是"新增了一个能用的功能"，而不是"修了一个 bug"。按用户感知来归类。

## 格式

标题、发布日期、一句话总述，然后按**主题**分节。每节下用无序列表，一条一句话说完。

```markdown
# MengkaNT v1.1.6 更新日志

发布日期：2026-08-01

本次更新新增发送短信验证方式，并优化了版本检查的响应速度。

## 账号安全验证

- 新增「发送短信」验证方式，可用密保手机发送指定内容完成验证，适用于收不到验证码的情况。
- 短信验证失败时会展示服务器返回的具体原因，便于排查。

## 插件接口

- 插件 API `get_sms` / `check_sms` 新增必填参数 `verify_type`，用于指定短信验证类型：`4` 为接收短信，`3` 为发送短信。
- 上述两个接口现在返回完整的验证信息。

## 其他优化

- 关于页面的版本号加载不再受网络状况影响。
```

主题名按本次内容自拟，贴近用户使用的场景（如「账号登录」「消息收发」「插件接口」「界面优化」）。改动少时可以用「新增功能」「问题修复」「其他优化」这类通用分类。

补充说明：

- **措辞**：用"支持""新增""修复""优化"开头，不用"重构""调整""改为"这类开发者视角的词。
- **技术名词**：只保留用户能查到的（插件 API 名、配置项名、界面上的功能名），用反引号标注。
- **数量**：宁可少而清楚。多条内部改动如果对用户是同一件事，合并成一条。
- **破坏性变更**：单独成节并写在最前面，明确说明用户要做什么。

```markdown
## ⚠️ 重要变更

- 插件 API `get_sms` / `check_sms` 新增必填参数 `verify_type`，未传该参数的插件将无法调用，请对照文档更新插件代码。
- 本次更新后需重新初始化数据库，升级前请备份 `data` 目录。
```

需要用户手动操作的事项（重新初始化、重新配置、备份数据）一定要写清楚，否则用户升级后遇到报错会不知所措。

## 版本号规范

遵循[语义化版本](https://semver.org/lang/zh-CN/)，三段数字，每段的含义是固定的：

```
v1.2.3
 │ │ └── PATCH  向后兼容的问题修复
 │ └──── MINOR  向后兼容的新功能
 └────── MAJOR  不兼容的变更
```

怎么判断该升哪一段：

| 本次改动 | 升哪段 | 示例 |
| --- | --- | --- |
| 只修复问题，用法完全不变 | PATCH | v1.2.3 → v1.2.4 |
| 新增功能、新增插件 API、给接口加**可选**参数 | MINOR | v1.2.3 → v1.3.0 |
| 移除或改变已有接口、给接口加**必填**参数、需要用户手动迁移数据 | MAJOR | v1.2.3 → v2.0.0 |

**低位归零**：升 MINOR 时 PATCH 归零，升 MAJOR 时 MINOR 和 PATCH 都归零。不要出现 `v1.3.5 → v2.0.5` 这种。

**PATCH 位不要当迭代号用。** 它专门留给"上一个版本发出去后发现问题，赶紧修一版"的情况。有新功能就升 MINOR，哪怕功能很小 —— MINOR 涨得快是正常的，把新功能塞进 PATCH 才是错的，会让用户误以为只是修 bug 而跳过更新。

**判断标准是用户视角的兼容性，不是改动量。** 改了几千行但用户用法不变，是 PATCH；只加了一个必填参数，就是 MAJOR。

### 预发布版本

需要先给部分用户试用时，用 `-` 加标识：

```
v1.3.0-beta.1
v1.3.0-rc.1
```

发布这类版本时**必须在 GitHub 上勾选 `Set as a pre-release`**。客户端只取最新的正式发行版，勾选后预发布版不会被推送给普通用户；不勾选则所有用户都会收到更新提示。

## Tag 与发布资产

Tag 名就是版本号，形式为 `vX.Y.Z`（如 `v1.3.0`），必须与程序内的版本号完全一致。客户端按这个格式比较版本，格式不对会导致更新检查异常。

发布时上传 `build.ps1 all` 生成的三个压缩包，**文件名保持不变**：

```
mengka-nt-vX.Y.Z-windows-amd64.zip
mengka-nt-vX.Y.Z-linux-amd64.tar.gz
mengka-nt-vX.Y.Z-linux-arm64.tar.gz
```

客户端靠文件名后缀识别对应平台的更新包，改名会导致自动更新找不到文件。
