# 萌卡 NT v2.4.0

## 管理员认证改为本地账号

- 初始化和后台登录不再依赖萌卡 NT 官网账号或个人令牌。
- 新实例在初始化页面直接设置本地管理员账号和密码，密码使用 bcrypt 哈希保存在实例本地数据库。
- 从旧官网管理员版本升级后，首次访问会强制进入一次本地管理员初始化；无需验证旧官网身份。
- 初始化迁移会撤销旧管理会话并清除旧官网身份字段，但保留 QQ 账号、指纹、节点、插件连接、配置与业务数据。
- 已使用本地 bcrypt 管理员的实例继续沿用原账号密码，不重复初始化。

## 插件市场改为本地导入

- 控制台将“插件市场”替换为“插件导入”，不再获取官网插件清单，也不再提供远程下载和在线更新。
- 管理员可上传 ZIP、TAR.GZ 或 TGZ 成品包；框架会保存到实例预留插件目录并生成管理卡片。
- 上传同一插件的更高版本会沿用原卡片、数据目录和回滚流程。
- 保留托管插件的启动、停止、卸载、日志、数据目录和 Web 管理端配置。
- 插件对接页面继续支持正向 WebSocket、反向 WebSocket、服务令牌和插件管理端 SSO。

## 协议目录

- 新增 Android 手机与平板 `9.1.70` 协议条目。
- 保留 Android `9.2.70` 与 Linux 协议条目。

## 框架版本策略

- 框架版本限制改由算法系统统一管理，不再依赖已经下线的旧官网插件。
- 算法系统可分别设置当前版本与最低允许版本；低于最低版本的框架会提示并要求升级。
- 策略服务临时不可用时会放行现有框架，避免网络故障把实例锁死。
- 框架仅通过 `qsign.bilibilibot.com` 获取策略，不包含算法服务器源站 IP。

## 升级说明

- 升级前停止框架并完整备份 `data` 目录。
- 完整替换程序和前端资源后启动框架，首次访问按提示设置本地管理员账号和密码。
- 不要删除或覆盖原有 `data`、`plugins`、日志及自定义文件。
- 旧版官网管理员令牌和登录会话在本地管理员初始化后失效。
- 插件市场入口不再提供；请从可信来源取得插件成品包，并在“插件导入”页面手动上传。

## 本版概览

- 新增本地管理员初始化与升级迁移流程；
- 新增本地插件导入、运行管理、日志和数据目录管理；
- 新增 Android 9.1.70 协议条目，并保留 Android 9.2.70 与 Linux；
- 新增算法服务统一的框架版本策略；
- 修复旧官网身份残留、插件入口和版本策略不可用时的用户提示问题。
