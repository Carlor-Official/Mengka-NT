# 萌卡 NT v2.2.3

## 修复

- 修复 QQ 空间未处理计数查询将 `ret=0、msg=""` 的无计数成功响应误报为业务失败，导致运行日志反复刷屏的问题。此类响应保留已有计数状态。
- 空间计数查询持续失败时逐步延长重试间隔，重复警告每 30 分钟汇总一次，恢复后自动回到正常轮询。
- 限制异常轮询间隔，避免过密请求；停止或重新登录后，旧轮询不再继续输出过期警告。

## 升级说明

- 升级前备份数据目录，替换程序及前端资源后重启框架。
- 保留原配置、管理员绑定和账号数据。本版继续保留新官网 `https://mknt.7ml.top` 及旧管理员绑定的登录兼容修复。
- 无插件 API 参数或返回契约变更。
