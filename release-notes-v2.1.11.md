# 萌卡 NT v2.1.11

- 调整 `buy_pet_bath_item`：直接返回商城实际的业务结果码 `result` 和订单号 `order_id`，不再要求结果码为 0、订单号非空。
- 支持 protobuf 缺省返回：省略的结果码和订单号分别返回 `0`、空字符串，订单号文本原样保留。
- 同步框架调试说明和 Node.js SDK 文档，明确由插件判断购买业务结果。

## 升级说明

- 插件不能再仅依赖“调用没有抛错”判断购买成功，需要检查业务返回并按需查询库存。`submitted: true` 仅表示请求已提交并收到可解析的 OIDB 成功响应；`effect_verified: false` 表示框架没有核验购买效果。
- 网络、响应解析和 OIDB 外层错误仍会报错，例如 `1000316`。购买请求不会自动重试。`count > 0` 和商品编号的编码规则保持不变。
- 本版不调整好友宠物资料查询及 `1000120` 的处理，也不修改其他宠物接口。
- 升级前备份框架配置和业务数据；手动升级时同时替换主程序与 `public`，保留 `data` 中的现有配置和数据。

[购买洗护用品接口说明](https://github.com/Carlor-Official/Mengka-NT/blob/v2.1.11/docs/api/buy_pet_bath_item.md)
