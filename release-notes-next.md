# 下一版本变更

当前正式版本为 [v2.4.0](release-notes-v2.4.0.md)，下一版本为 **v2.4.1**。

## 新增

- 新增 `native-ipc-v1` 原生插件传输，由框架直接安装、启动、停止和监控插件进程。
- 新增 Schema 3 原生安装包，包内声明入口、Action 权限、事件权限和最低框架版本。
- 新增框架生成的插件配置页，敏感字段本地加密保存，配置使用修订号避免并发覆盖并支持运行中热更新。
- Node.js SDK 新增原生插件运行时，同时保留正向、反向 WebSocket 接入。
- 本地导入新增可信来源确认和安装后程序文件指纹；目录内容被修改时阻止再次启动。
- 插件更新新增权限差异核对：新增 Action、事件权限、Web 管理端或传输方式变更必须由管理员二次确认。

## 升级说明

- v2.4.0 及更早版本不能运行 Schema 3 原生插件。
- 原生插件应声明 `min_framework: 2.4.1`；已有 WebSocket 插件不需要迁移。
- 插件版本替换仍由用户在“插件导入”页面手动上传，不恢复官网插件清单或在线市场。
- 原生插件仍以框架所属系统用户运行，不提供完整操作系统沙箱；只应安装可信来源的成品包。
