# 下一版本变更

当前正式版本为 [v2.3.8](release-notes-v2.3.8.md)。

## 等级加速

- “来元宝P图一次”遇到腾讯元宝安全校验时，框架后台返回受控的官方验证入口，等级加速页面直接展示 `yuanbao.tencent.com` 验证窗口，并支持完成验证后继续原任务。
- 同一验证入口会由框架通过私聊消息发送给触发任务的当前 QQ 自身，用户可在管理页面或 QQ“私聊自己”中打开；收件人由框架固定，不能由前端或插件指定。
- 元宝验证地址严格限制为腾讯官方 HTTPS 页面，不记录独立验证码或上游响应；验证成功后的元宝会话仅在框架进程内短期复用，不写入配置或数据库。
