# 萌卡 NT v1.9.5

本版本集中修复插件消息回调与多账号运行时的 SQLite 写入稳定性。

## 核心更新

- 修复 1.9.x 中插件接收不到消息与事件回调的问题，恢复好友消息、群消息及通知事件的正常分发。
- 修复多账号消息并发写入统计时偶发的 `SQLITE_BUSY`，降低高消息频率下的统计丢失与错误日志。
- 保持 Android 与 Linux QQ 的独立账号、登录和签名链路不变。
- 保留 1.9.4 的专属 Key 选择、固定绑定与安全传递功能。

## 升级说明

升级前请完整备份原目录中的 `data`。停止旧版本后替换程序文件和 `public` 前端资源，并保留原有数据库与管理员配置。

外发包不包含数据库、管理员配置、日志、源码、密钥或 Git 元数据。
