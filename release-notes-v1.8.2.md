# 萌卡 NT v1.8.2

本版本在 1.8.1 的现代管理端、SQLite 运行边界和专属 Key 机制上，补全 QQ 宠物公开 API，并将官网、正向 SDK、反向 SDK 统一到 194 个公开 API。

## QQ 宠物公开 API

- QQ 宠物分组全部恢复为普通公开插件 API，不再要求专属 Key，也不进入专属 Key 调用日志。
- 新增 `get_pet_medal_gallery`：获取全部勋章及获得、佩戴状态。
- 新增 `get_pet_attributes`：获取力量、智力、魅力等成长属性。
- 新增 `get_pet_interaction_messages`：获取来访、投喂等互动动态。
- 新增 `get_pet_pk_strangers`：获取宠物服务推荐的陌生人 PK 对手。
- 新增 `get_pet_fatigue_status`：获取冒险选项下发的疲劳警示。
- QQ 宠物公开 API 总数由 25 个增至 30 个，保留原有 action 名、请求字段、响应结构和协议处理。

## 宠物信息补全

- 宠物资料补充生日、性别、性格图标、等级和勋章等可用字段，并保留服务端缓存回退。
- 洗护目录和活动状态补充服务端已经返回的资源、步骤、警告、子事件和职业信息。
- 活动选项选择保持稳定顺序，避免同一批量操作因返回顺序变化选错目标。

## 专属 Key

- 专属 Key 受控目录现在只保留 `send_packet`。
- 已经仅授权 QQ 宠物接口的旧 Key 会按失败关闭原则停用；混合授权 Key 会移除 QQ 宠物权限并保留 `send_packet` 权限。
- Key 与框架实例联合校验、审批和审计规则保持不变；`send_packet` 的 `self_id`、`cmd`、`data`、`rsp`、`reserve` 等传输字段没有改动。

## API 与 SDK

- 官网、正向 Node.js SDK、反向 Node.js SDK 均同步为 194 个公开 API。
- 194 个公开 API 统一作为萌卡 NT 原生能力展示，不再标记为其他框架兼容接口。
- `send_group_notice`、`del_group_notice`、`mark_all_as_read`、`handle_quick_operation` 已按公开 action 名直接注册；旧内部名称仅用于保障既有插件平滑升级。
- API 文档示例、详情跳转和 SDK 调用名统一使用公开 action，参数契约与 1.8.2 处理函数逐项核对无差异。
- QQ 宠物文档增加完整参数、返回值和错误边界；官网“专属 Key”分组只展示 `send_packet`。
- 框架版本和管理前端可见版本更新为 1.8.2。

## 安全与运行边界

- 外发包继续使用本地 SQLite 与进程内缓存，不引入 PostgreSQL、Redis、旧用户门户、商城支付管理或 QQ 账号授权到期体系。
- 软件包不包含数据库、管理员配置、专属 Key、日志、源码或 Git 元数据。
- 本次没有改写 QQ 协议字段、插件 WebSocket 信封或既有 API 传输参数。

## 升级方法

1. 停止正在运行的萌卡 NT，并完整备份原目录中的 `data`。
2. 下载与操作系统、CPU 架构匹配的 1.8.2 软件包并核对 `SHA256SUMS.txt`。
3. 从 1.8.1 升级时替换程序和 `public` 前端资源；保留原有运行数据与管理员配置。
4. 启动框架并确认后台显示 1.8.2；使用 `send_packet` 时继续在管理员头像菜单绑定算法系统签发的专属 Key。

从更旧的大版本升级时建议使用新目录部署，不要用外发包中的初始文件覆盖现有数据库。真实 QQ 功能仍受账号状态、客户端协议和 QQ 服务端状态影响。
