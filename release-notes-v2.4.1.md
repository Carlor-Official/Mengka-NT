# 萌卡 NT v2.4.1

## 新增

- 原生 `native-ipc-v1` 插件运行方式；
- 原生插件安装包身份、权限和配置声明；
- 插件配置页面与运行中配置更新；
- 原生插件运行状态、生命周期和权限变更提示；
- Node.js、C++ SDK 原生插件运行时支持。
- 随附用户系统 `2.0.17` 原生 Linux AMD64 外发包，最低框架版本为 `2.4.1`。

## 保持兼容

- 正向 WebSocket、反向 WebSocket 和现有插件对接方式继续保留；
- v2.4.0 及更早版本的普通 WebSocket 插件无需修改；
- 插件仍通过「插件导入」手动上传，不恢复官网插件市场。

## 升级提示

- 原生插件包的最低框架版本为 `2.4.1`；
- 升级前请停止框架并备份 `data` 目录；
- 原生插件只应安装来自可信来源的成品包；
- 具体 API、部署和 SDK 用法以官网文档为准。
