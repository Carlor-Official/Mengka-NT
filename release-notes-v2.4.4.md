# 萌卡 NT v2.4.4

## 修复

- 修复“听歌领金币兑换加速”因 QQ 音乐余额查询结果不准确而误报金币不足的问题；框架改为以兑换接口的业务结果判断成功或金币不足。
- 修复重复刷新或连续执行时频繁获取 QQ 音乐登录态、触发换码限流并返回 `100050` 的问题。

## 优化

- 同一账号短时复用 QQ 音乐登录 Cookie，并合并并发登录请求。
- 上游返回 `100050` 时进入 10 分钟冷却，冷却期间不再重复发起换码请求，并返回明确提示。

## 升级说明

- 本次更新不增加插件参数，也不改变等级任务 action；插件继续按任务标题调用 `execute_level_task_selection`。
- 任务是否完成以刷新后的 QQ 等级面板为准，请勿在超时或结果不明确时自动重放。
- 升级前请停止框架并备份 `data` 目录。
