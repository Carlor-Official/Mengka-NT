# 萌卡 NT v2.3.2

## 新增

- 等级任务在擂台小游戏明确返回“微信未授权登录”时显示授权入口。
- 授权弹窗使用任务返回的 QQ 擂台跳转地址生成二维码，并支持直接打开 QQ 授权页。
- 用户完成一次授权后可返回页面刷新任务，后续优先复用已登录 Android 会话。

## 规则

- 普通网络错误、QQ 会话错误、启动失败或擂台创建结果不确定不会显示授权入口。
- 二维码是 QQ 擂台入口，不是独立的微信登录二维码；微信授权仍在 QQ 内的小游戏页面完成。
- 继续使用原有 `get_level_tasks` 与 `execute_level_tasks` 接口，不新增分数或授权凭证参数。

## 升级

框架与前端资源需一起升级。升级后重新打开等级任务页面即可看到授权入口；现有账号、任务状态和授权会话保持不变。
