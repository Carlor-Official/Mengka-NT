# 萌卡 NT v2.3.9

## 新增

- 无新增公开 API。

## 优化与修复

- 将“来元宝P图一次”恢复为“暂不支持”，不再显示额外说明。
- 停止该任务的手动执行和自动计划，旧客户端提交时也不会执行。
- 升级启动后清理该任务的旧会话、验证状态及自动计划选择。

## 验证

- 后端全量 Go 测试通过。
- 前端 118 项测试、生产构建通过。
- Node.js SDK 51 项测试通过。
- Windows AMD64、Linux AMD64 与 Linux ARM64 外发包通过启动检查。
- `git diff --check` 通过。

## 升级说明

- 升级前停止框架并备份 `data` 目录，完整替换框架程序和前端资源后重启。
- 保留原配置、账号数据、管理员绑定、插件数据和其他等级任务状态。
- 本版沿用现有等级任务 API，无需修改插件调用参数。
