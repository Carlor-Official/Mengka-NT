# 萌卡 NT v2.1.10

- Linux 托管插件改为独立低权限进程，每个插件使用自己的普通用户运行，不再依赖 bubblewrap、命名空间或 seccomp，解决部分宝塔、容器和旧安装环境升级后无法启动插件的问题。
- 默认托管目录自动迁移至 `/var/lib/mengka-managed/<安装标识>`，复制程序和数据后统一切换数据库路径，保留旧目录作为迁移前副本，避免网站目录归属导致插件启动失败。自定义外部数据目录保持原位。
- 完善迁移与启动恢复：中途失败保留数据，后续启动继续恢复；框架升级不等待插件依赖安装。主动停止、取消恢复、卸载或授权失败的插件不会被自动启动。
- 停止插件、插件主进程退出或框架异常退出后，自动回收该插件的后台子进程，避免残留进程继续运行。
- 保留官网审核上架、安装包校验、插件授权、WS 对接、日志与 Web 管理入口。公开 API、SDK 参数与返回结构不变。

## 升级说明

1. 升级前停止框架与托管插件，备份框架 `data`、已有的 `/var/lib/mengka-managed/<安装标识>`、自定义外部数据目录及网关配置。手动升级时同时替换主程序与 `public`，保留配置和业务数据。
2. Linux 托管插件仍要求框架服务以 root 运行，用于创建专属用户、切换身份和管理进程；插件业务程序以普通用户运行。非 root 的宝塔项目或 systemd 服务需管理员调整运行用户后重启，框架不会自动提权或修改系统服务用户。缺少 `useradd`、`setpriv` 等工具时会尝试通过系统软件源准备；失败不阻止框架升级，插件保留待恢复状态并提示原因。
3. 此运行方式依靠 Linux 用户和文件权限保护数据。插件可看到宿主公开文件、权限允许的进程信息，并访问宿主可达的网络服务；允许目录清单不再提供操作系统只读挂载或文件隐藏。请正确设置框架、账号数据及其他服务的文件权限。
4. 迁移后旧目录不会继续同步新数据。回滚前停止框架及插件，恢复与旧程序匹配的完整备份，或执行明确的数据回迁；不能仅替换旧版可执行文件。不要覆盖运行中的 SQLite 文件。
5. Windows 与分离式 WebSocket 插件的运行方式保持不变。

[托管插件与迁移说明](https://github.com/Carlor-Official/Mengka-NT/blob/v2.1.10/docs/managed-plugins.md) · [官网 API 文档](https://mknt.net/api/)
