# 萌卡 NT v2.3.1

## 修复

- 修复小游戏擂台创建成功后，QQ 本人列表或房间详情短暂未刷新时被误判为失败的问题。创建后最多进行 6 次只读确认，每次间隔 500 毫秒；创建请求始终只发送一次。
- 区分创建前准备失败、创建请求失败、创建结果回读失败和创建后启动信息失败，避免统一显示“擂台创建结果未确认”。
- 为擂台任务增加固定阶段、失败分类和 QQ 数字错误码的脱敏诊断，并同步写入目标账号“近期日志”。日志不记录凭证、房间标识或原始响应。
- 取消擂台任务的每日尝试限制和 `attempted_today` 响应字段。失败会返回具体原因并保持“待完成”，用户可再次执行；旧失败记录无需清理。
- 只有 QQ 独立刷新确认任务完成后才显示“已完成”。同一账号仍禁止并发执行，每轮执行使用新的内部标识，并且单轮只发送一次创建请求。

## 保持不变

- 继续使用 `get_level_tasks` 和 `execute_level_tasks`，没有新增或更名公开 action，也没有修改请求参数。
- 继续使用 1–99 的随机整数分数和独立 QQ 完成状态回读；页面和 SDK 不会自动重试失败任务。

## 升级说明

- 升级前停止框架并备份现有 `data` 目录，完整替换程序及前端资源后重启；保留原配置、账号、管理员绑定、托管插件数据和等级任务记录。
- 框架与前端应一起升级。旧客户端不能再依赖擂台任务的 `attempted_today` 字段；应根据 `can_execute`、`is_done`、`status_text` 和返回错误展示状态。
- 本版修复通过本地合成延迟场景和完整测试验证；由于历史客户日志缺少旧版未保存的阶段信息，不将该合成场景作为任意客户历史故障的唯一根因。
