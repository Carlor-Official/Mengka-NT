# 萌卡 NT v2.0.8

## 新增

- 左侧菜单在“容器”和“设置”之间新增“调试”。支持搜索、分类选择 API，填写参数或编辑完整 JSON。
- 调试页面采用参数、请求样式、响应样式三栏布局，适配浅色、深色和手机屏幕。
- 支持 JSON、Python、JavaScript 和 cURL 请求预览与复制；参数变化只刷新预览，点击“发送请求”后才发起一次真实调用。
- 响应支持原始文本、格式化 JSON、复制、耗时、状态和流式分片展示；支持停止等待，不自动重放失败或超时请求。

- 专属 Key API 显示权限标记；框架未激活 Key 或 Key 不可用、未授权该 API 时禁止调试。页面与后端均检查，普通 API 保持可用。

## 使用与升级

- 前后端需一起更新。调试前选择一个已启动的正向 WS 服务；没有服务时先到“插件 → 服务列表”添加。
- 调试沿用当前服务认证、账号协议选择、专属 Key 和审计链路；现有公开 action 及 SDK 调用参数不变。
- 详细步骤和请求预览说明见[在线 API 调试](docs/api-debugger.md)。
