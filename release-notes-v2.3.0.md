# 萌卡 NT v2.3.0

## 新增与优化

- “等级加速”接入任务 80“创建小游戏擂台并取得成绩”。框架通过 Go 直接协议完成方块冲刺 v6 的擂台创建、1–99 随机整数分数上报、退出和 QQ 等级任务回读，不运行小游戏，也不需要 Python、Node、Chrome 或外部 worker。
- 任务 80 继续使用 `get_level_tasks`、`execute_level_tasks` 和原有参数。页面会按服务端状态显示可执行、已完成、今日已尝试或“微信未授权登录”；当天持久尝试不会自动重放。
- 插件市场安装和在线更新复用框架在线更新的 GitHub 下载节点。框架先用小范围请求检测直连与镜像节点，整包按顺序下载；失败时切换下一节点并最终回退原站。非 GitHub 来源保持直连，安装包大小、SHA-256、签名和安装校验保持不变。
- 官网、身份认证、插件市场和开发文档入口更新为 `https://mknt.rezcwl.com`，旧官网标准地址会在升级时自动迁移。

## 升级说明

- 升级前备份数据目录，完整替换程序和前端资源后重启框架；保留原配置、管理员绑定、账号数据、等级任务每日记录及托管插件数据。
- 本版没有新增或更名公开 action，也没有增加分数、游戏、计时参数。请同步更新仓库中的 Node.js SDK 和当前 API 文档。
- 正式外发包不包含旧擂台 worker、游戏资源或浏览器运行工具。
