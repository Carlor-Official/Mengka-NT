发布日期：2026-08-05

本次更新完善账号管理、消息媒体与插件能力，新增红包和等级任务支持，并提升登录、实时状态与在线更新的稳定性。

## 账号与管理后台

- 新增「静默模式」，可暂停处理实时消息推送，同时保持账号连接。
- 新增手动删除离线账号和定时清理功能，可配置清理间隔；在线或正在登录的账号会自动跳过。
- 账号设置新增隐私模式、缓存登录、静默模式和离线账号清理选项，并支持按在线、离线和登录中状态筛选账号。
- 优化账号状态、在线时长和消息指示器的实时同步，减少页面显示与实际运行状态不一致的情况。
- 优化账号日志查看体验，打开日志后自动跟随最新内容，手动向上查看时不再强制跳到底部。
- 优化节点管理，正在使用的节点需先停用后才能编辑或删除，减少误操作。
- 修复 HTTP 等非安全上下文中复制账号信息、验证链接和插件内容失败的问题。

## 消息、媒体与红包

- 完善好友与群聊语音、视频及视频封面的接收和播放支持，提升媒体地址获取兼容性。
- 新增 QQ 群红包消息识别、红包详情查询和领取能力。
- 优化账号登录、离线与缓存登录状态切换，减少登录完成后仍显示登录中或离线状态异常的问题。
- 提升账号安全验证的连接兼容性，减少部分网络环境下验证请求失败的情况。

## 插件接口

- 新增插件 API `set_qq_avatar`，支持通过本地路径、`file://` 或 HTTP(S) 地址设置账号头像。
- 新增插件 API `get_red_packet_info` 与 `grab_red_packet`，用于查询和领取收到的 QQ 群红包。
- 新增插件 API `publish_qzone_feed`、`get_bot_info` 与 `execute_level_tasks`，支持发布空间动态、查询 Bot 运行信息和执行等级加速任务。
- 优化插件操作的并发处理，耗时操作不再阻塞同一插件的其他请求。
- 正向与反向 WebSocket Node.js SDK 已同步新增接口，并延长图片、头像和红包等操作的等待时间。

## 等级任务

- 等级加速任务新增「浏览十条空间好友动态」，可与现有空间发布、好友动态点赞等任务一起执行。

## 更新与发行

- 发行仓库迁移至 `Carlor-Official/Mengka-NT`，关于页面已同步新的仓库入口。
- 优化在线更新连接稳定性：GitHub 更新检查或更新包直连失败时，会自动切换镜像线路并重试。

## 下载

- Windows x64：`mengka-nt-v1.2.0-windows-amd64.zip`
- Linux x64：`mengka-nt-v1.2.0-linux-amd64.tar.gz`
- Linux ARM64：`mengka-nt-v1.2.0-linux-arm64.tar.gz`
